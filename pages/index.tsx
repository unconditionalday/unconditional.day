import type { NextPage } from "next";
const Home: NextPage = () => {
  return (
    <div
      className={"w-full h-full flex flex-col items-center justify-center"}
    ></div>
  );
};

export default Home;
